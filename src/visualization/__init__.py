"""Plotting for metrology engineers: scenes, error ellipsoids, visibility,
and network-solve results.

Not one of the six build steps in CLAUDE.md §5 -- it's tooling alongside
the chain, not a link in it -- see `plotting.py`'s module docstring for
why it's built this way and what it deliberately isn't.
"""
